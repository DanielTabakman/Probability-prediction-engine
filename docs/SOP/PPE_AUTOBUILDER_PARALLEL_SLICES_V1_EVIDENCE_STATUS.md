# PPE autobuilder parallel slices v1 — evidence

**Status:** COMPLETE

- `read_build_locks()` in `scripts/ppe_remote_build_agent.py` (single + `locks[]` format)
- Autobuilder lock summary exposes `lock_count` when multiple locks active
