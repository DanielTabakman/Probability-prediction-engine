# Agent routing v1

**Plane:** CONTROL-PLANE · **Purpose:** one page — which docs to load by thread role, and what to never load.

**SSOT for verdict → command:** [`PPE_CANONICAL_OPERATOR_SCRIPTS_V1.md`](PPE_CANONICAL_OPERATOR_SCRIPTS_V1.md) · human double-click: [`OPERATOR_BUTTON_MAP.md`](OPERATOR_BUTTON_MAP.md)

**Thread openers:** [`THREAD_STARTERS_V1.md`](THREAD_STARTERS_V1.md) · rules: [`.cursor/rules/ppe-thread-roles.mdc`](../../.cursor/rules/ppe-thread-roles.mdc) · discovery: [`.cursor/rules/sop-discovery.mdc`](../../.cursor/rules/sop-discovery.mdc)

**Doc discovery CLI (charter / ad-hoc topics):**

```bash
python scripts/resolve_sop.py --topic "<phrase>" --json
python scripts/resolve_sop.py --chapter <chapter_id> --json
python scripts/resolve_sop.py --module <module_id> --json
python scripts/resolve_sop.py --role <operator|charter|ide_build|explore|neutral> --json
python scripts/resolve_sop.py --list-topics --json
```

| Flag | Purpose |
|------|---------|
| `--topic` | Match phrase → program/SOP bundle (`TOPIC_ROUTES`) |
| `--chapter` | Relay chapter → program + sprint + selection paths |
| `--module` | Module id → `*_PROGRAM_V1.md` |
| `--role` | Thread role → default `load_always` / `never_load` |
| `--list-topics` | Print topic route catalog |

Machine index: [`CHAPTER_DOC_INDEX.json`](CHAPTER_DOC_INDEX.json) · archive catalog: [`ARCHIVE_INDEX.md`](ARCHIVE_INDEX.md) (generated; **do not load for BUILD**) · regenerate: `generate_chapter_doc_index.cmd --write` · stale check: `--check` (gate)

**Chapter mode (CLOSEOUT_ONLY vs IDE_BUILD):** read `Mode:` line in `artifacts/orchestrator/OPERATOR_STATUS.md` — generated by `scripts/ppe_chapter_mode.py`.

---

## Load order by role

| Role | Open with | Load (in order) | Never load |
|------|-----------|-----------------|------------|
| **operator** | `what's next?` or `THREAD_ROLE: operator` | `OPERATOR_STATUS.md` → `AGENT_CONTINUITY_BRIEF.md` → `ACTIVE_PRODUCT_DIRECTION.json` | Full UX charter unless asked; do not implement IDE_BUILD in-thread when burst uses `@ppe-director` |
| **ide_build** | `IDE BUILD thread` + `@IDE_BUILD_STARTER_*.md` | Starter file only | `OPERATOR_STATUS`, burst plan, relay queue |
| **charter** | `Charter thread` + one program doc | Stated program/SELECTION doc only | `OPERATOR_STATUS`, `BURST_PLAN.json`, `@ppe-director` |
| **explore** | `Explore thread` / review only | User @ files only | Relay, commits unless asked |
| **neutral** | User question | Answer scope only | Do not lead with operator status |

### Operator thread — BUILD load order

When implementing product (rare — prefer `@ppe-build-worker`):

1. [`REPO_LAYER_MAP_V1.md`](REPO_LAYER_MAP_V1.md) or BUILD packet `LAYER_PRESET`
2. Active sprint spec
3. [`AGENT_CONTINUITY_BRIEF.md`](AGENT_CONTINUITY_BRIEF.md)
4. Layer frontier: [`MVP1_FRONTIER.md`](MVP1_FRONTIER.md) or [`MSOS_FRONTIER.md`](MSOS_FRONTIER.md)

---

## Verdict + Mode → action

Before any relay command on desktop: `python scripts/ppe_loop_host_guard.py --check` — if `"allowed": false`, use desktop row only.

| Verdict | `Mode` in status | Desktop | VM |
|---------|------------------|---------|-----|
| `IDE_BUILD` | `IDE_BUILD` | `DESKTOP_BUILD.cmd` → starter → gate → commit → mark ready | Wait for desktop |
| `IDE_BUILD` | `CLOSEOUT_ONLY` | Do **not** re-implement — finish marker/closeout | `run_ppe_local.cmd` |
| `RUN_LOCAL` | any | **`DESKTOP_CONTINUE.cmd`** | `run_ppe_local.cmd` / `@ppe-finish-worker` |
| `RUN_AUTO` | any | SSH status only | `run_ppe.cmd` or loop |
| Preflight warnings | — | Resolve dirty tree / branch first; [`RECOVERY_PROTOCOL.md`](RECOVERY_PROTOCOL.md) if mixed-plane | Same |

**Common mistake:** `RUN_LOCAL` on desktop ≠ `run_ppe_local.cmd`.

---

## UX agents

| Task | Agent | Thread |
|------|-------|--------|
| UX design, backlog, philosophy | `@ppe-ux-charter` | **charter** |
| Execute UX backlog through relay | `@ppe-ux-director` | **operator** only |

Relay queue wins over [`UX_EXECUTION_BACKLOG_V1.md`](UX_EXECUTION_BACKLOG_V1.md) when they conflict.

---

## Steering SSOT (do not copy-paste long warnings)

| Field | Source |
|-------|--------|
| Closeout-only chapters | [`AGENT_STEERING_V1.json`](AGENT_STEERING_V1.json) → `closeoutOnlyChapterIds` |
| Live chapter mode | `OPERATOR_STATUS.md` → `Mode: CLOSEOUT_ONLY` |
| Next BUILD candidate | `AGENT_STEERING_V1.json` → `nextBuildCandidateId` + UX backlog |
| Steward next action | Composed from `AGENT_STEERING_V1.json` via `scripts/ppe_chapter_mode.py` |

Do **not** re-BUILD product when status shows `CLOSEOUT_ONLY` or starter has closeout banner.

---

## Recovery & delegation

| Situation | Doc / action |
|-----------|--------------|
| Mixed-plane dirty tree | [`RECOVERY_PROTOCOL.md`](RECOVERY_PROTOCOL.md) — repair only, no product scope |
| Coordination uncertain / burst blocked | `python scripts/ppe_coordination_check.py --write` → `@ppe-coordination-check` |
| Before steering/canon commit | `python scripts/ppe_delegation_envelope.py --gate-check` |
| Asset enable / batch wave | [`.cursor/rules/asset-auto-discovery.mdc`](../../.cursor/rules/asset-auto-discovery.mdc) — run `discover_asset_data_source.py --json` first |
| Unclear which SOP to load | `python scripts/resolve_sop.py --topic "<phrase>" --json` · [`.cursor/rules/sop-discovery.mdc`](../../.cursor/rules/sop-discovery.mdc) |

---

## Topic → load (charter threads)

Run `resolve_sop.py` when unsure; table below is the fast path.

| Topic / operator phrase | Load only |
|-------------------------|-----------|
| UX execution / design | [`UX_EXECUTION_BACKLOG_V1.md`](UX_EXECUTION_BACKLOG_V1.md) + [`MSOS_UX_DESIGN_PHILOSOPHY_V1.md`](MSOS_UX_DESIGN_PHILOSOPHY_V1.md) |
| Trader learning spine | [`TRADER_LEARNING_SPINE_PROGRAM_V1.md`](TRADER_LEARNING_SPINE_PROGRAM_V1.md) |
| Asset enable / hook up X | Run `discover_asset_data_source.py --asset X --json` — [`ASSET_DATA_SOURCE_DISCOVERY_V1.md`](ASSET_DATA_SOURCE_DISCOVERY_V1.md) |
| Asset batch wave / tier-1 | [`ASSET_BATCH_EXPANSION_POLICY_V1.md`](ASSET_BATCH_EXPANSION_POLICY_V1.md) |
| Tradeable universe / catalog | [`PPE_TRADEABLE_UNIVERSE_PROGRAM_V1.md`](PPE_TRADEABLE_UNIVERSE_PROGRAM_V1.md) |
| Options Horizon | [`OPTIONS_HORIZON_PROGRAM_V1.md`](OPTIONS_HORIZON_PROGRAM_V1.md) |
| Exposure menu | [`EXPOSURE_MENU_PROGRAM_V1.md`](EXPOSURE_MENU_PROGRAM_V1.md) |
| Forward consistency | [`FORWARD_CONSISTENCY_RADAR_PROGRAM_V1.md`](FORWARD_CONSISTENCY_RADAR_PROGRAM_V1.md) |
| Cross-venue quant / collectors | [`MVP1_CROSS_VENUE_QUANT_PROGRAM_V1.md`](MVP1_CROSS_VENUE_QUANT_PROGRAM_V1.md) |
| MSOS platform P0–P8 | [`MSOS_WEBSITE_PROGRAM.md`](MSOS_WEBSITE_PROGRAM.md) |
| Commit / test gates | [`COMMIT_POLICY.md`](COMMIT_POLICY.md) |
| Deploy / VPS | [`PRODUCTION_DEPLOY_PROTOCOL.md`](PRODUCTION_DEPLOY_PROTOCOL.md) |
| Relay chapter by id | `python scripts/resolve_sop.py --chapter <id> --json` |

---

## Relay subagents

| Agent | When |
|-------|------|
| `@ppe-director` | Operator burst — spawns build/finish/triage workers |
| `@ppe-coordination-check` | Burst blocked (`direct_action: coordination_check`) or before risky steering/queue edits |
| `@ppe-build-worker` | `IDE_BUILD` slice implementation |
| `@ppe-finish-worker` | `RUN_LOCAL` closeout |
| `@ppe-triage-worker` | `FIX_PLAN` / `STALE_STATE` / `ERROR` (max 1) |

Coordination SSOT: [`CHAPTER_COORDINATION_V1.md`](CHAPTER_COORDINATION_V1.md) · artifact: `artifacts/control_plane/COORDINATION_CHECK.json`

---

## Factory policy (no skills layer)

PPE uses **rules + generated status + starters + subagents**, not Cursor Agent Skills, for operator workflows. Do not add a parallel skills hierarchy unless a ritual fails after script/rule fixes ([`BUILD_FACTORY_BOUNDARY_V1.md`](BUILD_FACTORY_BOUNDARY_V1.md)).

Built-in Cursor skills (PR babysit, security review) are optional for generic tasks only.

---

## Anti-patterns

| Avoid | Use instead |
|-------|-------------|
| Opening UX chat with no role | `Charter thread` + program doc |
| `@ppe-ux-director` for brainstorming | Charter + `@ppe-ux-charter` |
| Steward + operator + BUILD in one thread | Separate threads per role |
| Pasting `OPERATOR_STATUS` into charter threads | One-line pointer to operator thread |
| Re-implementing product when `CLOSEOUT_ONLY` | Finish relay / `DESKTOP_CONTINUE` |
