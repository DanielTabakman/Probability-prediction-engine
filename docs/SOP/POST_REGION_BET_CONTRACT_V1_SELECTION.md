# Post-selection record: Region Bet contract v1

## COORDINATION STATUS

SELECTED as Autobuilder catalog order **6**, immediately after terminal `ppe_equity_universe_tier1c_v1` (catalog order 5 / PR #5434).

## Selection Evidence

- Founder-approved product intent backlog: Region Bet is the durable product object.
- Prior Autobuilder drafts #5427 (horizon comparison) and #5428 (expression fit ranking) remain open drafts — they are **not** this item and must not be recreated here.
- Existing `HorizonRegionIntent` is insufficient as the full Region Bet contract.

## Scope Boundary

This selection authorizes only the contract/persistence slice listed in `SPRINT_REGION_BET_CONTRACT_V1.md`. It does not authorize guided UI, compare/rank bridges, monitor teaching, session resume, archive replay, or merge of #5427/#5428.

## Order

Catalog order 6 follows order 5. Future items 07–17 stay intent-only until JIT packetization against then-current `main` after predecessors become terminal.
