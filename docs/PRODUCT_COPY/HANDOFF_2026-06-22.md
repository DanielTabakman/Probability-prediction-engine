# Copy agent handoff — 2026-06-22 (from BUILD/UX thread)

**For:** dedicated MSOS copy agent thread · **Not for:** relay BUILD unless wiring imports

**Load with:** [`MSOS_COPY_AGENT_V1.md`](../SOP/MSOS_COPY_AGENT_V1.md) · [`MSOS_PUBLIC_COPY_V1.md`](../SOP/MSOS_PUBLIC_COPY_V1.md) · skill `.cursor/skills/msos-copy-editor/`

---

## What this thread already shipped

| PR / commit theme | What changed |
|-------------------|--------------|
| #281 | Full `msos-shell` trader copy pass — fixtures, Strategy Lab, Command Center, monitor/history; removed PPE/workflow/fixture jargon from visitor strings |
| #282 | Test witness fixes after copy pass |
| #283 | `MSOS_PUBLIC_COPY_V1.md` — voice, banned terms, checklist |
| #284 | Homepage trader positioning — hero, product cards, Command Center preview, four-step flow |
| #278 | Belief presets (Higher/Lower/More vol/Less vol) — copy in `beliefPresets.ts` + Strategy Lab panels |

**Production walkthrough URL:** https://marketstructureos.com/strategy-lab

**Deploy witnesses:** HTTP + Playwright run after deploy; they catch integration breaks, **not** tone.

---

## Main positioning (operator-approved)

**Platform promise:** Market Structure OS helps traders compare their thesis with what the market already implies, locate meaningful disagreement, and explore possible trade expressions without hiding assumptions.

**Job-to-be-done (read this before any surface edit):**

> I have a market view. What does the options market imply? Where do I disagree? Is there a structure that fits my thesis?

**North star (15-second test):** See what BTC options imply, where you disagree, and what payoff fits.

---

## Homepage — done (copy agent maintains, don't re-litigate without operator)

- **Approved packet:** [`packets/homepage.v1.md`](packets/homepage.v1.md) (`status: approved`)
- **Content file:** `apps/msos-web/src/content/homepage.ts` — components import this
- **Canon table:** `MSOS_PUBLIC_COPY_V1.md` § Homepage canon

Key decisions already locked:

- Hero CTA primary: **Explore the platform** → Strategy Lab route
- Visible engine name: **Probability Engine** (never “PPE” on homepage)
- Preview metric label: **Thesis gap** (not “Disagreement located” on homepage)
- Compliance pill: **Expression, not recommendation**
- Beta CTA default: **Join research beta** (`researchOfferCta.ts`)

**Optional polish (operator taste, not blockers):** hero body is slightly dense on mobile; primary CTA could point to Command Center instead of Strategy Lab if operator prefers hub-first.

---

## Your priority queue (copy agent — next surfaces)

| Priority | Surface id | Why | Likely stale strings |
|----------|------------|-----|----------------------|
| 1 | `public-nav` | Above the fold on every page | “Market surfaces”, “Enter Command Center” — consider “Monitor” / “Open Command Center” |
| 2 | `strategy-lab` | Primary demo | Belief presets OK; confirm/expression panels may still feel internal; don’t promise chart moves on preset click |
| 3 | `command-center` | Operator saw degraded UX | Copy sanitized for sqlite `bindings` errors; full packet still needed for happy-path strings |
| 4 | `monitor` | Nav label mismatch | Fixtures + `MonitorContent.tsx` |
| 5 | `history` | Journey tail | `HistoryContent.tsx` + feeds |
| 6 | `learn` | Not in inventory yet | Vision/architecture-heavy — **add surface to `SURFACE_INVENTORY.json` before drafting** |

---

## Copy vs product truth (do not over-promise)

| Shipped in copy/UI | Not shipped yet — avoid implying |
|--------------------|----------------------------------|
| Belief presets update outcome text + legend | Teal belief curve on PPE chart does **not** move yet |
| Strategy Lab live Deribit metrics (when API up) | Command Center snapshot/history DB in Docker (`better-sqlite3` bindings) — degraded state possible |
| Paper / demo / preview labels | Live order placement |
| “Live demo” tags | Full production parity on all surfaces |

When writing degraded paths: use `publicCopy.ts` helpers (`friendlySnapshotFeedMessage`) — never raw stack traces or module names.

---

## Operator ↔ agent process (this thread)

| Machine (CI / agents) | Human (operator) |
|-----------------------|------------------|
| HTTP + Playwright witnesses after deploy | Subjective UX: pleasant? confusing? show to a friend? |
| Fix obvious jargon/copy without asking | Steer taste; narrate feedback while browsing |
| `validate_msos_public_copy.py` banned-term scan | Read-aloud pass on mobile + desktop |
| Don’t ask operator to catch HTTP 500s | Desktop pass better for layout; mobile still useful |

**Operator feedback pattern that worked:** “This line cringes” or “reads like my notes” → add term to banned table in `MSOS_PUBLIC_COPY_V1.md` + fix in same pass.

---

## Grep targets (audit any surface)

Search visitor paths and replace where appropriate:

- `company and software platform`
- `probability/thesis engine`
- `Company and platform identity`
- `Fit, not recommendation` (homepage now: **Expression, not recommendation**)
- `Preview includes BTC options through PPE`
- `Read surfaces`
- `expression families`
- `calibration memory`
- `Disagreement located` (homepage → **Thesis gap**; deeper product may keep “disagreement”)
- Raw `PPE` in visible UI (OK in code/env/comments)

---

## Technical notes for copy agent

- **Edit strings in:** `docs/PRODUCT_COPY/packets/` → promote to `apps/msos-web/src/content/*.ts` or `publicCopy.ts`
- **Do not edit:** `components/` unless operator explicitly says “wire imports” (that's BUILD)
- **Tests:** assert wiring/structure, not exact marketing copy — update witnesses only when routes or CTA hrefs change
- **Validator:** `python scripts/validate_msos_public_copy.py`
- **Research beta label:** env `NEXT_PUBLIC_PPE_RESEARCH_OFFER_LABEL` overrides default “Join research beta”

---

## BUILD backlog (for separate thread — not copy agent)

- Wire any remaining inline component strings to content files (homepage/nav largely done)
- Command Center sqlite in Docker image
- Mobile Command Center panel reorder (awaiting operator desktop layout notes)
- Belief preset → PPE chart overlay (Python contract; no TS math)

---

## Suggested first message in copy agent thread

```text
Load docs/PRODUCT_COPY/HANDOFF_2026-06-22.md and run a banned-term audit.
Start with public-nav (Market surfaces / Enter Command Center), then charter strategy-lab packet.
```
