# VISION_TEMPLATE

Reusable skeleton for **phase** or **initiative** vision docs. Copy to e.g. `docs/VISION/PHASE_VISION_<NAME>.md` and fill. Remove unused optional blocks.

---

## Title
`<Short name — e.g. Phase 2: Opportunity surfacing>`

## Horizon
- [ ] **Phase vision** (cycle-scoped)  
- [ ] **Initiative vision** (cross-cutting slice)

## Phase / initiative summary
`<One paragraph: what we are building toward in this slice, in user-outcome terms.>`

## User outcomes
`<Bullet list: what the user can understand, decide, or do when this lands.>`

## Experience bar
`<What “good” feels like on the primary surface(s) — pace, clarity, trust.>`

## UX priorities (this slice)
1.  
2.  
3.  

## Semantic / labeling priorities (this slice)
`<Terms that must stay precise; reference `docs/SEMANTIC_CONTRACTS.md` or add a delta note if new.>`

## Must-not-break (this slice)
-  
-  

## Deferred explicitly
-  
-  

## Drift risks to watch
-  
-  

## Visual references (optional)

Use when layout or density matters. **Pair every asset with text** (what changed, what is in/out of scope).

| Asset | Path | Notes |
|--------|------|--------|
| e.g. wireframe | `docs/VISION/assets/<slug>.png` | |
| e.g. annotated screenshot | `docs/VISION/assets/<slug>.png` | |

**Storage convention:** put images under `docs/VISION/assets/` (create folder if needed). Name files `YYYY-MM-DD_<short-slug>.png` or similar. In this doc, **only** link paths and describe intent—do not embed secrets or PII.

## Links
- Long horizon: `VISION_MASTER.md`  
- Contracts: `docs/SEMANTIC_CONTRACTS.md` (if applicable)  
- Cycle spec: `docs/SOP/ORIGINAL_SPEC.md` (if applicable)  

## Last updated
YYYY-MM-DD by `<name>`
