# Production Docker image (Streamlit + display API) — minimal runtime deps.
# Dev/CI uses requirements.txt (playwright, pytest, google sync are dev-only).
python-dotenv>=1.0.0
pyyaml>=6.0
pandas>=2.0.0
numpy>=1.24.0
yfinance>=0.2.28
requests>=2.31.0
streamlit>=1.33.0
plotly>=5.18.0
