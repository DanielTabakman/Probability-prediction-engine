---
description: Relay runbook detail — load on demand. Verdict auto-execute lives in ppe-operator-core.mdc.
alwaysApply: false
---

# PPE unified run (on demand)

**Always-on operator behavior:** [`ppe-operator-core.mdc`](ppe-operator-core.mdc)

Load for full relay/operator runbook detail.

**Operator layout:** [`PPE_OPERATOR_LAYOUT_ADR.md`](docs/SOP/PPE_OPERATOR_LAYOUT_ADR.md)

| Machine | Loop | After IDE BUILD |
|---------|------|-----------------|
| **VM** | `run_ppe_headless_stack.cmd` / auto loop | `run_ppe_local.cmd` |
| **Desktop** | Off | `DESKTOP_BUILD.cmd` → merge → `DESKTOP_CONTINUE.cmd` |

- Local profile: `run_ppe_auto_local_loop.cmd` on **VM only** ([`PPE_AUTO_OPERATOR.local.json`](docs/SOP/PPE_AUTO_OPERATOR.local.json)).
- With API credits: `run_ppe_auto_acp_loop.cmd` — do not run without budget.

**Steward:** update `ACTIVE_PHASE_MANIFEST.json` or `PHASE_QUEUE.json` at SELECTION; do not paste orchestrator logs.

**Closeout:** `post_relay_continue` marks queue DONE; read `LAST_RUN_REPORT.md`; new thread with continuity brief only.

Canonical runbook: [`RELAY_ORCHESTRATOR_RUNBOOK_V1.md`](docs/SOP/RELAY_ORCHESTRATOR_RUNBOOK_V1.md).
